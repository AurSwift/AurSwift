# NPM configuration for native module building
# Cross-platform configuration for better-sqlite3, serialport, node-hid, usb

# Build configuration  
build-from-source=true
cache_max=5120000000
cache_min=10

# Windows-specific settings (ignored on Mac/Linux)
# Developers on Windows should set these via: npm config set msvs_version 2022
# target_platform=win32
# target_arch=x64
# msvs-version=2022

# Python configuration
# Windows: npm config set python python3
# Mac/Linux: Usually detected automatically
python=python3